language: python
python:
  - "3.6"
install:
  - pip install -r requirements.txt
  - pip install -e src
  - curl -o src/server/prod_secrets.json $SECRETS_URL
script:
  - pytest
branches:
  only:
  - master
  - develop
