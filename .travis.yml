language: python
python:
  - "3.6"
install:
  - pip install -r requirements.txt
  - pip install -e src
  - echo "travis" > src/server/prod_secret.txt
script:
  - pytest
branches:
  only:
  - master
  - develop
  